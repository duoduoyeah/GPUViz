The graph will show the gpu architecture use cytoscape 

1. it will get the necessary input like,
current level of the graph,
what component it will shows
for each component: the shape, the color(the default color is, all the component has
the same color for simplicity)

