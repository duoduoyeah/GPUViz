I'll generate the two state management files for you with clean, minimal code.I've generated both store files with the necessary functionality:

**gpuStore.ts:**
- Manages GPU configuration (memory size, core count, shader arrays, clock speed)
- Stores generated GPU components
- `updateConfig`: Updates configuration values
- `generateArchitecture`: Creates GPU components based on current config

**graphStore.ts:**
- Manages graph visualization data (nodes and edges)
- `setGraphData`: Sets both nodes and edges at once
- `updateNodePosition`: Updates individual node positions (useful for drag functionality)

Both stores use Zustand for state management and include all the TypeScript interfaces needed. The code is minimal and focused on the essential functionality without any extra features.